fastapi==0.140.7
# starlette is fastapi's ASGI core, but fastapi only floors it (>=0.46), which
# lets resolvers pick 0.52.x — that line carries several PYSEC advisories
# (PYSEC-2026-161/248/249/2280/2281) fixed in 1.3.1. Test suite passes on 1.x.
starlette>=1.3.1,<2
uvicorn[standard]==0.51.0
# click is uvicorn's CLI dep; 8.3.1 has PYSEC-2026-2132, fixed in 8.3.3.
click>=8.3.3,<9
aiosqlite==0.22.1
aiofiles==25.1.0
cryptography==50.0.1
pyyaml==6.0.3
itsdangerous==2.2.0
python-multipart==0.0.32
requests==2.34.2
urllib3==2.7.0
# idna is transitive via requests/httpx/anyio; <3.15 has PYSEC-2026-215.
idna>=3.15
netmiko==4.7.0
# ntc-templates is a transitive dep of netmiko but used directly via
# send_command(..., use_textfsm=True) in the mac_tracking CLI collector.
# Pinning here keeps the dependency intentional rather than coincidental.
ntc-templates>=7,<10
pyrad==2.5.4
# python-ldap requires OpenLDAP headers (libldap/lber.h) which Windows does
# not ship. Skip on Windows; the LDAP auth backend in netcontrol/routes/auth.py
# is wrapped in a try/except and degrades to LDAP_AVAILABLE=False there.
# <3.4.5 has two ldap.filter.escape_filter_chars bypasses (GHSA-p34h-wq7j-h5v6,
# GHSA-r7r6-cc7p-4v5m); the LDAP auth backend builds filters from user input.
python-ldap>=3.4.5,<3.5; sys_platform != "win32"
pysnmp>=7.1.26,<7.2
# pyasn1 is pysnmp's BER/CER/DER decoder - it parses untrusted SNMP input.
# <0.6.4 has PYSEC-2026-3455/3456/3457 (GHSA-m4p7-r5rc-7g4j et al).
pyasn1>=0.6.4
# ICMP liveness probes for the monitoring poll and ICMP-only host onboarding.
# icmplib runs unprivileged on both Linux (SOCK_DGRAM) and Windows, so the
# Plexus process does not need to be elevated.
icmplib>=3.0.4,<4
ansible-runner>=2.4.3,<2.5
# Floor pydantic so resolvers can't pick 2.9.x (paired with pydantic-core
# 2.23.4 which uses a PyO3 that doesn't support Python 3.14). 2.11+ ships
# pydantic-core ≥2.33 with current PyO3.
pydantic>=2.13.3,<3
# httpx is used at runtime by netcontrol/routes/ipam_adapters.py, so it
# belongs in the runtime requirements (was previously dev-only).
httpx>=0.28.1,<1
# asyncpg powers the postgres backend (APP_DB_ENGINE=postgres). The default
# docker-compose deploy sets this, so asyncpg must be present in the image.
asyncpg>=0.30,<1
