-r requirements.txt
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
# Per-test wall-clock cap: a wedged event loop fails in minutes with a
# traceback instead of hanging the whole run (see pytest.ini [timeout]).
pytest-timeout==2.4.0
# Parallel test execution: run `pytest -n auto` (~4 min vs ~18 min serial)
pytest-xdist==3.8.0
ruff==0.15.21
mypy==2.2.0
pre-commit==4.6.0
types-requests==2.33.0.20260518
pip-audit==2.10.1
bandit==1.9.4
# safety 3.5.2 pinned pydantic<2.10, blocking Python 3.14 (which needs
# pydantic-core ≥2.27 with PyO3 ≥0.23). 3.7.0 dropped that cap.
safety>=3.8.1,<4
