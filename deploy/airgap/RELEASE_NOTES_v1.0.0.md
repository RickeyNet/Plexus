# Plexus v1.0.0

First public release of Plexus — a Python-first network automation control center inspired by SolarWinds and Ansible Tower/AWX. Designed to simplify network management and actually work. Manage device inventories, run automation playbooks, store config templates, and stream live job output through a REST API with WebSocket support.

This release is the result of a long internal development cycle (see `CHANGELOG.md` for the development history); the 1.0 milestone marks it as ready for production deployment.

## What Plexus does

- **Inventory & device discovery** — group devices by role/site, discover hosts via SNMP, track serial numbers and metadata.
- **Automation playbooks** — Python `BasePlaybook` subclasses run as async generators with real-time WebSocket-streamed log events.
- **Config management** — scheduled config backups, drift detection with revertable history, search across historical backups.
- **Compliance scans** — on-demand and scheduled compliance checks against your devices.
- **IPAM & DHCP** — VLAN/VRF-aware subnet scoping, bi-directional reconciliation with external IPAM systems, DHCP integration with Kea / Windows DHCP / Infoblox.
- **Topology** — L2/L3 topology built from CDP/LLDP with FDB+ARP fallback, STP multi-VLAN visualization with anomaly detection.
- **Digital twin / lab mode** — clone production devices into isolated containerlab-backed labs (Arista cEOS, Nokia SR Linux, FRR, Linux), apply changes with risk scoring, run multi-device topologies end-to-end, detect drift between twins and live production.
- **Device upgrades** — schedule and orchestrate device firmware/software upgrades with state that survives Plexus restarts.
- **Auth** — local users, LDAPS / Active Directory, and RADIUS providers with group-based admin mapping.

## Deployment

Two supported paths:

### Online (`docker compose`)
For VMs with internet access:
```bash
git clone <repo-url> /opt/plexus && cd /opt/plexus
bash deploy/setup.sh         # generates .env + self-signed cert
docker compose up -d
```
See [deploy/DEPLOYMENT.md](../DEPLOYMENT.md) for the full walkthrough.

### Air-gapped
For VMs with no internet, this release ships a self-contained bundle:
```bash
# On a connected machine
bash deploy/airgap/bundle.sh                       # produces plexus-airgap.tar.gz
# Transfer the tarball to the offline VM, then on the VM:
tar -xzf plexus-airgap.tar.gz
cd plexus-airgap-bundle
sudo bash install.sh
```
The bundle includes the Plexus image, postgres + nginx images, Docker Engine `.deb`s, and optional XFCE+xrdp+AD-join packages. See [deploy/airgap/README.md](README.md) and [deploy/airgap/VM_SETUP.md](VM_SETUP.md) for the full operator walkthrough including static-IP netplan templates, RDP setup, and AD domain join.

### Database
Plexus supports both SQLite (default, simplest) and PostgreSQL (recommended for production). Set `APP_DB_ENGINE=postgres` and `APP_DATABASE_URL` to use Postgres. A migration utility (`tools/migrate_sqlite_to_postgres.py`) moves data between backends with row-count and optional checksum verification.

## Requirements

- Ubuntu 22.04 / 24.04 / 26.04 (or RHEL/Rocky 9) for the host VM
- 2 vCPU, 4 GB RAM, 40 GB disk minimum
- Docker 24+ with the `compose` plugin
- Python 3.14 inside the container (handled by the image — no host Python needed when using Docker)

## First login

After `docker compose up -d`, browse to `https://<vm-ip-or-hostname>` and log in:

- Username: `admin`
- Password: `netcontrol`

You will be forced to change the password on first login. Then configure your authentication provider (LDAP/AD or RADIUS) under Settings → Authentication Provider.

## Security notes

- `netcontrol.key` (the Fernet credential encryption key) is auto-generated on first launch and stored in the Docker named volume `plexus-db`. **Back it up.** Losing it permanently breaks decryption of every stored device credential.
- API token enforcement is opt-in via `APP_REQUIRE_API_TOKEN=true` in `.env`.
- Self-registration is disabled by default; toggle via `APP_ALLOW_SELF_REGISTER`.
- HTTPS is provided by the bundled nginx reverse proxy with a self-signed cert generated by `deploy/setup.sh`. Replace with your CA-signed cert per the deployment guide for production.

## Known limitations

- Drag-and-drop on the multi-device lab topology canvas is deferred to a follow-on; the current canvas uses circular auto-layout.
- The frontend is mid-migration to React: most pages are React, the chrome and a handful of pages still use the legacy SPA. Both work side-by-side; no operator action needed.

## What's in the box

| File / artifact                  | Purpose                                          |
|----------------------------------|--------------------------------------------------|
| Source code (this repo)          | Plexus app, playbooks, frontend, deploy scripts  |
| `plexus-airgap.tar.gz.part-*`    | Air-gapped bundle (rejoin with `cat *.part-* >`) |

The air-gapped bundle is split into ~1.9 GB chunks to fit GitHub Release file-size limits. Rejoin with:

```bash
cat plexus-airgap.tar.gz.part-* > plexus-airgap.tar.gz
```

## Full changelog

See [CHANGELOG.md](../../CHANGELOG.md) for the per-feature breakdown of this release and the pre-1.0 development history.

## License

MIT.
